/**
 * Contexto <b>identity</b>: organizaciones, usuarios y RBAC (ADMIN/OPERATOR/AUDITOR).
 *
 * Capas: {@code domain} (POJOs + puertos) · {@code application} (use cases) ·
 * {@code infrastructure} (web + persistencia JPA).
 */
package com.semtex.identity;
